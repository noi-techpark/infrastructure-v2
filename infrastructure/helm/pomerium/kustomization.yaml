resources:
  - github.com/pomerium/ingress-controller/config/default?ref=v0.32.5
  - certificate.yaml
  - idp-secret.yaml

patches:
  - path: service-patch.yaml
    target:
      kind: Service
      name: pomerium-proxy
