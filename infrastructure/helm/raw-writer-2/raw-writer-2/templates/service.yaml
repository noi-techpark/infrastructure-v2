apiVersion: v1
kind: Service
metadata:
  name: {{ include "raw-writer-2.fullname" . }}
  labels:
    {{- include "raw-writer-2.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "raw-writer-2.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
